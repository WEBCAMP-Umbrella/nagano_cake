<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>NaganoCake</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@300;400;500;700&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

  <body>

    <header class="sticky-top">
        <nav class="navbar navbar-expand-sm navbar-light bg-light">

          <% if request.path.include?('admin') %>
            <%= render 'layouts/admin',admin: @admin %>
          <% else %>
            <%= render 'layouts/customer',customer: @customer %>
          <% end %>
        </nav>
    </header>

    <div class="container mt-5">
      <%= yield %>
    </div>


      <footer class="border-top mt-5">
        <div class="container mt-5">
          <div class="row">
            <dl class= "col-sm-4 text-center">
              <dt class= "footer_dt">支払い方法</dt>
              <dd class="footer_dd">クレジット</dd>
              <dd class="footer_dd">銀行振り込み</dd>
            </dl>
            <dl class= "col-sm-4 text-center">
              <dt class= "footer_dt">ご購入にあたって</dt>
              <dd class="footer_dd">はじめての方へ</dd>
              <dd class="footer_dd">よくあるご質問</dd>
              <dd class="footer_dd">ご利用ガイド</dd>
            </dl>
            <dl class= "col-sm-4 text-center">
              <dt class= "footer_dt">ながのCakeについて</dt>
              <dd class="footer_dd">アクセス</dd>
              <dd class="footer_dd">営業時間</dd>
              <dd class="footer_dd">会社概要</dd>
            </dl>
          </div>
        </div>

        <div class="jumbotron　h-25" style="background-color: #fb5454">
          <div class="text-center">©nagano.cake</div>
        </div>
      </footer>
  </body>
</html>
