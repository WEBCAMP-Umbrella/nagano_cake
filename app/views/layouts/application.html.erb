<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>NaganoCake</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://fonts.googleapis.com/css2?family=Baloo+Bhaina+2:wght@600;800&family=Noto+Sans+JP:wght@100;300;400;500;700;800&family=M+PLUS+Rounded+1c:wght@900&display=swap" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" type="text/css" href="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.css"/>

    <script type="text/javascript" src="//cdn.jsdelivr.net/npm/slick-carousel@1.8.1/slick/slick.min.js"></script>
  </head>

  <body class="d-flex flex-column" style="min-height: 100vh">

    <header class="sticky-top">

          <% if request.path.include?('admin') %>
            <%= render 'layouts/admin',admin: @admin %>
          <% else %>
            <%= render 'layouts/customer',customer: @customer %>
          <% end %>
    </header>
    <div class="container mt-5 mb-auto">

      <%= yield %>
    </div>

    <div id="page_top"><i class="fas fa-arrow-up"></i></div>

      <footer class="border-top mt-5">

        <% if request.path.include?('admin') %>
          <%= render 'layouts/admin_footer',admin: @admin %>
        <% else %>
          <%= render 'layouts/customer_footer',customer: @customer %>
        <% end %>

        <div class="jumbotron　h-25" style="background-color: #fb5454">
          <div class="text-center text-white">©nagano.cake</div>
        </div>
      </footer>
  </body>
</html>
